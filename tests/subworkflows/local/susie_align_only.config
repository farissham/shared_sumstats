// Integration-test the SUSIE_FINEMAP wiring without the susieR container:
// run the SUSIE step in --align_only mode (join + allele alignment only).
process {
    withName: 'SUSIE' {
        ext.args = '--align_only'
    }
}
